# Add project specific ProGuard rules here.
# Currently unused — isMinifyEnabled is false for the release build type
# (see app/build.gradle.kts) until the app is feature-complete and can be
# tested against a minified build before shipping. This file exists so the
# proguardFiles() reference doesn't fail once minification is turned on.
