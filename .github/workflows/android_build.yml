name: AEZEL Android App CI/CD Build

on:
  workflow_dispatch:
  push:
    branches:
      - android-app
      - main
  pull_request:
    branches:
      - android-app
      - main

jobs:
  build-android-app:
    name: Build AEZEL Android APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Set up Java 17 (JDK)
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Make Gradlew Executable
        run: chmod +x gradlew

      - name: Build Android Self-Signed Release & Debug APKs
        run: ./gradlew assembleRelease assembleDebug --stacktrace

      - name: Upload Self-Signed Release APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: aezel-vcu-app-release
          path: app/build/outputs/apk/release/app-release.apk
          if-no-files-found: error

      - name: Upload Debug APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: aezel-vcu-app-debug
          path: app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
